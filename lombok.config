# Don't look for further config files above this directory.
config.stopBubbling = true

# Marks generated methods @lombok.Generated so JaCoCo skips them. Without this ~30
# generated methods count as untested production code. See CLAUDE.md.
lombok.addLombokGeneratedAnnotation = true
