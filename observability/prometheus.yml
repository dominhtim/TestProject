global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "my-spring-app"
    metrics_path: "/actuator/prometheus"
    static_configs:
      # Compose service name. Running the app on the host instead? Use
      # host.docker.internal:8080.
      - targets: ["app:8080"]
